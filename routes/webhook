var express = require('express');

var router = express.Router();

router.route('/')
    .get(function (req, res) {
    if (req.query['hub.verify_token'] === 'todomanager') {
        res.send(req.query['hub.challenge']);
    }
    res.send('Error, wrong token');
});

module.exports =router;